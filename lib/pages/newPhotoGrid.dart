import 'package:flutter/material.dart';

class NewPhotoGrid extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("New"),
      ),
    );
  }
}
